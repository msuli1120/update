<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>TEST</title>
  </head>
  <body>
    {% if results is not empty %}
      {% for result in results %}
        <li><a href="/person/{{ result.id }}">{{ result.name }}</a></li>
        <br>
      {% endfor %}
    {% endif %}
  </body>
</html>
